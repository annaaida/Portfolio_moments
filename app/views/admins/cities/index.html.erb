<!-- ヘッダー部分テンプレート -->
<%= render 'layouts/admins-header' %>
<br>
<br>
<br>
<div class = "container">
  <div class = "row">
  	<table class = "table">
  	  <h3>都市一覧</h3>
  	  <thead>
       <tr>
        <th>都市</th>
        <th></th>
       </tr>
      </thead>
      <tbody>
      	<% @cities.each do |c| %>
      	 <tr>
      	  <td class = "countents"><%= c.city_name %></td>
      	  <td class = "contents"><%= link_to 'edit', edit_admins_city_path(c.id), class: "review-button btn btn-success" %></td>
      	 </tr>
      	<% end %>
      	<%= paginate @cities, class: "paginate" %>
  	</table>
  </div>
</div>