<div class = "container">
  <div class = "row">
   <%= form_for @country, url: edit_admins_country_path do |f| %>
    <div class = "row col-xs-offset-2">
     <div class = "img_form">
	  <%= f.label "国の写真" %>
	  <%= f.attachment_field :country_img %>
	  <%= f.submit "編集完了", class: "btn btn-warning" %>
   <% end %>
  </div>
</div>